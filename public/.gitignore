# IMPORTANT: public/ contains generated files only
# All source files belong in assets/ or other directories
# Everything is inlined into index.html during build

# Ignore everything in this directory (including index.html)
*

# EXCEPT docs/ directory (generated documentation)
!docs/
!docs/**

# And this .gitignore file itself
!.gitignore

# Note: index.html is now gitignored (see root .gitignore line 92)
# It is generated fresh on each CI run and should be regenerated locally
# To regenerate: ./scripts/regenerate_html.sh
